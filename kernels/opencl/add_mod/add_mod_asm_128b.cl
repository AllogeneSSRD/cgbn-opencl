// AUTO-GENERATED by tools/gen_mp_addsub_bits_stage1.py - do not edit.
static inline void add_mod_asm_128b_body(uint *r, const uint *a, const uint *b, const uint *N) {
    uint ca = 0u, cs = 1u;
    asm_fused_block4_priv(a, b, N, r, ca, cs, &ca, &cs);
}

#if !defined(__AMDGCN__)
static inline void add_mod_unroll_128b_body(uint *r, const uint *a, const uint *b, const uint *N) {
    ulong carry_add = 0ul;
    ulong carry_sub = 1ul;
    {
        ulong sum = (ulong)a[0] + (ulong)b[0] + carry_add;
        carry_add = sum >> 32;
        ulong temp = (ulong)(uint)sum + (ulong)(~N[0]) + carry_sub;
        carry_sub = temp >> 32;
        r[0] = (uint)temp;
    }
    {
        ulong sum = (ulong)a[1] + (ulong)b[1] + carry_add;
        carry_add = sum >> 32;
        ulong temp = (ulong)(uint)sum + (ulong)(~N[1]) + carry_sub;
        carry_sub = temp >> 32;
        r[1] = (uint)temp;
    }
    {
        ulong sum = (ulong)a[2] + (ulong)b[2] + carry_add;
        carry_add = sum >> 32;
        ulong temp = (ulong)(uint)sum + (ulong)(~N[2]) + carry_sub;
        carry_sub = temp >> 32;
        r[2] = (uint)temp;
    }
    {
        ulong sum = (ulong)a[3] + (ulong)b[3] + carry_add;
        carry_add = sum >> 32;
        ulong temp = (ulong)(uint)sum + (ulong)(~N[3]) + carry_sub;
        carry_sub = temp >> 32;
        r[3] = (uint)temp;
    }
    if ((carry_add | carry_sub) != 0ul) {
        return;
    }
    ulong c = 0ul;
    {
        ulong s = (ulong)r[0] + (ulong)N[0] + c;
        r[0] = (uint)s;
        c = s >> 32;
    }
    {
        ulong s = (ulong)r[1] + (ulong)N[1] + c;
        r[1] = (uint)s;
        c = s >> 32;
    }
    {
        ulong s = (ulong)r[2] + (ulong)N[2] + c;
        r[2] = (uint)s;
        c = s >> 32;
    }
    {
        ulong s = (ulong)r[3] + (ulong)N[3] + c;
        r[3] = (uint)s;
        c = s >> 32;
    }
}

#endif
#if defined(__AMDGCN__)
static inline void add_mod_asm_128b(uint *r, const uint *a, const uint *b,
                                       const uint *N, uint limbs) {
    if (limbs == 4u) { add_mod_asm_128b_body(r, a, b, N); }
    (void)limbs;
}
#else
static inline void add_mod_asm_128b(uint *r, const uint *a, const uint *b,
                                       const uint *N, uint limbs) {
    if (limbs == 4u) { add_mod_unroll_128b_body(r, a, b, N); }
    (void)limbs;
}
#endif
