// AUTO-GENERATED by tools/gen_mp_addsub_bits_stage1.py - do not edit.
static inline int sub_mod_unroll_128b_body(uint *r, const uint *a, const uint *b, const uint *N) {
    ulong br = 0ul;
    {
        ulong av = (ulong)a[0];
        ulong bv = (ulong)b[0];
        ulong w = av - bv - br;
        r[0] = (uint)w;
        br = (av < bv + br) ? 1ul : 0ul;
    }
    {
        ulong av = (ulong)a[1];
        ulong bv = (ulong)b[1];
        ulong w = av - bv - br;
        r[1] = (uint)w;
        br = (av < bv + br) ? 1ul : 0ul;
    }
    {
        ulong av = (ulong)a[2];
        ulong bv = (ulong)b[2];
        ulong w = av - bv - br;
        r[2] = (uint)w;
        br = (av < bv + br) ? 1ul : 0ul;
    }
    {
        ulong av = (ulong)a[3];
        ulong bv = (ulong)b[3];
        ulong w = av - bv - br;
        r[3] = (uint)w;
        br = (av < bv + br) ? 1ul : 0ul;
    }
    if (br != 0ul) {
        ulong c = 0ul;
        {
            ulong s = (ulong)r[0] + (ulong)N[0] + c;
            r[0] = (uint)s;
            c = s >> 32;
        }
        {
            ulong s = (ulong)r[1] + (ulong)N[1] + c;
            r[1] = (uint)s;
            c = s >> 32;
        }
        {
            ulong s = (ulong)r[2] + (ulong)N[2] + c;
            r[2] = (uint)s;
            c = s >> 32;
        }
        {
            ulong s = (ulong)r[3] + (ulong)N[3] + c;
            r[3] = (uint)s;
            c = s >> 32;
        }
        return 1;
    }
    return 0;
}

static inline int sub_mod_unroll_128b(uint *r, const uint *a, const uint *b,
                                         const uint *N, uint limbs) {
    if (limbs == 16u) { return sub_mod_unroll_128b_body(r, a, b, N); }
    return 0;
}
